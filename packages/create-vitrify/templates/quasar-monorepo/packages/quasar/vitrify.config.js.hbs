import VitePlugin from 'quasar-plugin-{{ name }}/vite-plugin'

export default function ({ mode, command }) {
  return {
    plugins: [
      VitePlugin()
    ],
    vitrify: {
      hooks: {
        // Vitrify hooks
      },
      sass: {
        variables: {
          $primary: '#000000'
        }
      }
    },
    quasar: {
      extras: [
        'material-icons'
      ],
      framework: {
        components: [
          // Deprecated
        ],
        plugins: [
        ]
      },
    }
  }
}